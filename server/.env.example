# CrossBot — Environment config
# Copy to server/.env

# DeepSeek API Key (required for AI fallback)
AI_API_KEY=

# Optional overrides
AI_PROVIDER=deepseek
AI_MODEL=deepseek-chat

# Port for local dev
PORT=3456
